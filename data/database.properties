#here each developer needs to change user and password to their own accounts
#this file is added to .gitignore so it won't update the remote repository

db.url=jdbc:postgresql://localhost:5432/toy_store
db.user=karolina
db.password=cactus